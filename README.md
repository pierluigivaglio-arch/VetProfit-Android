# VetProfit Android
Applicazione Android offline di VetProfit. Package: `it.vetprofit.app`.

## APK
GitHub Actions compila automaticamente `VetProfit.apk` ad ogni push su `main`.
Aprire **Actions → Build VetProfit APK → ultimo run → Artifacts → VetProfit-APK**.
